# Process Control
These assembly files are for the purposes of learning process control in OS

To compile and run:
``` bash
make run FILE=file_name
```

Then just to run
``` bash
./file_name
```

NB! - must have an assembler, like NASM, to compile.
